# Build output
build/

# IDE
.idea/

# Runtime artifacts (placed next to the exe on the target machine)
tray-config.json
sing-box-tray.log

# sing-box config: holds proxy credentials, never commit it
config.json
